spring:
  datasource:
    username: postgres
    url:jdbc: postgresql://localhost:5432/restfull
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update